program correto;
int a, b;

begin
	a:= 5;
	read(b);

	if(b > 0)
		begin
			write(b);
			write(b);
			write(b)
		end;

	while(a > 0) {loop não está sendo executado}
		begin 
			b:=b+1; {b não está sendo incrementado}
			write(b);
			a:=a-1
		end
end.